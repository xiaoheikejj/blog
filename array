
shift（）删除数组第一个元素，返回元素
unshift（）数组第一个位置增加元素，返回新数组长度
slice（）两个参数，第一个参数起始位置数字，第二个参数结束位置，返回新数组，slice（）没有参数，拷贝数组
splice（）删除新数组中一部分，两个参数，第一个起始位置，第二个需要删除的个数，返回值被删除的元素。
map（）对数组所有元素传递给回调函数，返回一个新数组。forEach（）遍历数组所有成员，不返回值
filter（）创建一个新数组，参数是函数，函数用来筛选符合条件的所有元素
reduce（）从左到右累加成一个值，出入回调函数。reduceRight（）从右到左
indexOf（）返回元素在数组中第一次出现的位置。没有返回-1。lastIndexOf（）返回元素在数组中最后一次出现的位置


