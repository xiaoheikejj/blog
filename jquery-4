.hide('fast/slow')。保存元素的原始属性值
toggle()
slideDown('时间'，'回调')一个滑动动画来显示匹配的元素
slideUp()隐藏显示的元素
slideToggle()
fadeIn()淡入，fadeOut()淡出
fadeToggle()
fadeTo(duration,opacity,callback)
animate()自定义动画
.each()可以遍历数组和对象。.val()返回或设置被选元素的值
.inArray()一个指定的值是否存在数组中，存在返回索引，不存在返回-1
.trim()去除空格。字符串前后存在空格去除，字符串中间不去除
.get()获取集合中单独某元素
.index()让给定元素从集合中获取索引
