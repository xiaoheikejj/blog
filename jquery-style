.attr()获取和设置元素的属性
.removeAttr()移除某元素的属性
.text()返回一个字符串，包括所有匹配元素的合并文本。.html()处理的是元素内容，。text()处理的是文本内容
.val()主要处理表单元素的值，如input,textarea，select,读取表单内容的'value'值
.val()获取元素集合的第一个元素的当前值，val(value)每个元素的值，val(function)返回设置值的函数。
.addClass()只是添加样式类名到元素上，removeCalss()
.toggleClass()添加或删除一个或多个类名，.toggleClass（className,data）,data是布尔值，用来判断样式是否应该被移除/添加
.css()改变样式
.css()的样式高于.addClass()
.data(element,key,value)存数据，(element,key)静态接口，取数据，（key,value）实例接口，存数据，（key）实例接口，取数据
removeDate（）删除接口
//筛选选择器
$（':first'），$（':last'）匹配第一个和最后一个元素
$（':parent'）含子元素或者文本的元素
$（':empty'）没有子元素或者文本的元素
特殊选择器$(this)，var $this=$(this)
$(dom)将dom对象加工成jquery对象，就可以调用jquery当中的方法。
创建元素：document.createElement,设置属性：setAttribute,添加文本：innerHTML,加入文档：appendChild


任何数和NaN比较都返回false,任何涉及NaN的操作都返回NaN
在isNaN（）中true转化成1，false是0
Number()
相等和不相等：先转换后比较，全等和不全等：比较不转换
相等==；不相等！=
null和undefined是相等的；不能将null和undefined转化成其他任何值
全等操作符要两个操作数进行转化后比较；！==在两个操作数在不转化时不想等的情况下返回true
null==undefined返回true；因为他们是类似的值
null===undefined返回false；因为他们不是同类型的值


浮点数值的最高进度是17位小数，所以，永远不要测试某个特定的浮点数值
parseFloat()只能解析10进制，没有基数。可以解析后一个小数点；parseInt()反之
只有null和undefined没有toString()方法，string中的toString()方法返回string的一个副本
String()如果是null返回'null',如果是undefined返回'undefined'；其他数据类型调用toString()方法获得结果


isPrototypeOf()测试一个对象是不是在另一个对象的原型链上
instanceOf()测试一个对象在原型链中是否存在一个构造函数的prototype
Object.create(）使用指定的原型对象和它的属性创建一个新对象
Object.getPrototypeOf()返回指定对象的原型；内部[[prototype]]的值
hasOwnProperty()会忽视从原型链中继承的属性
keys()返回一个对象自己可以枚举的数组
getOwnPropotyNames()获取所有实例属性







